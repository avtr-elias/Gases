@using Gases.Controllers
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResources> SharedLocalizer
@{
    ViewData["Title"] = SharedLocalizer["UnpublishGeoTIFF"];
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>@SharedLocalizer["UnpublishGeoTIFF"]</h2>
<div class="row">
    <div class="col-md-4">
        <form asp-action="UnpublishGeoTIFF">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">@SharedLocalizer["Layer"]</label>
                <select name="LayerName" class="form-control" asp-items="ViewBag.GeoTIFFLayers"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="@SharedLocalizer["Unpublish"]" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>